server:
    port: 8087

eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://localhost:8081/eureka/
    fetch-registry: true
    register-with-eureka: true

spring:

jpa:
  show-sql: false
  ddl-auto: update
  properties:

jmx:
  enabled: false

datasource:
url: jdbc:mysql://localhost:3306/learning-microservice?useTimezone=true&serverTimezone=UTC&allowPublicKeyRetrieval=true&sslMode=DISABLED
username: root
password: lfs123!@#

#DEV ONLY
#logging:
#  level:
#    root: TRACE
